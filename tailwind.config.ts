import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#047857",
        secondary: "#B45309",
        background: "#FAFAFA",
        canvas: "#FAFAFA",
        card: "#FFFFFF",
      },
    },
  },
};

export default config;
